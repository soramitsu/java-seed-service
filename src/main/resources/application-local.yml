spring:
  profiles:
    include:
      - local-inmem

  cloud:
    vault:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false


static:
  welcome: "Hello"

dynamic:
  name: "Local User"
