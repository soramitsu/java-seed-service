spring:
  cloud:
    vault:
      enabled: true
      host: localhost
      port: 8200
      scheme: http
      connection-timeout: 5000
      read-timeout: 15000
      authentication: TOKEN
      token: "custom-token-change-me"
      fail-fast: true
      kv:
        enabled: true
